# Notes from data anaylytic class

# panda for python and dplyr for r are similar
#numpy for numbers, matplot lib ( plots) for base visualization
# plotly for r or python ( YOU NEED TO KNOW PLOTLY)
# IDE (jupyter lab/notebook, r studio, vs code)
# python is interpreted line by line ( not compiled like C++)
# Linux for dummies
# kernel is like a session
# TensorFlow uses images in ML
# .ipypb ( before jupyter there was i python)
# Check points are used for sort of version control
# Databrinks can write in all language
# break: exits loop
# continue: skips

#break
# Code Demo
for i in range(5):
    if i == 3:
        break
    print(i)
